<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="my.e.soilmoisturetemperarureproject">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SoilMoistureTemperarureProject">
        <activity android:name=".ShowDetails.UserAccountActivity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".Auth.ResetPasswordActivity"
            android:parentActivityName=".Auth.StartActivity" />

        <activity android:name=".MainActivity" />
        <activity
            android:name=".Auth.RegistrationActivity"
            android:parentActivityName=".Auth.StartActivity" />
        <activity
            android:name=".Auth.LoginActivity"
            android:parentActivityName=".Auth.StartActivity" />
        <activity
            android:name=".ShowDetails.SensorDataActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Auth.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>